# Bevy Level Order BFS

Starter crate for a binary tree level-order traversal (breadth-first search) visualization. The current version is a placeholder scene set up for incremental development.

## Quick start

```
cargo run
```
